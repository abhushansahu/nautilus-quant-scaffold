# Copy to .env and fill in values. NEVER commit .env.

# Deribit testnet/mainnet (primary — `trade-baby-trade paper --config configs/profiles/paper_btc.yaml`)
DERIBIT_API_KEY=
DERIBIT_API_SECRET=
DERIBIT_TESTNET=true

# Interactive Brokers paper/live gateway (secondary — `configs/profiles/paper_spy.yaml`)
IB_HOST=
IB_PORT=
IB_CLIENT_ID=
