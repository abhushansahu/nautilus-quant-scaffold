[project]
name = "trade-baby-trade"
version = "0.1.0"
description = "Personal trading project built on NautilusTrader."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nautilus-trader>=1.228.0",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
    "typer>=0.26.7",
]

[dependency-groups]
dev = []
